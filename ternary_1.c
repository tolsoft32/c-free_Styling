#include <stdio.h>
/**
 * main - Entry point
 * return - success (0) if program run well
 */
int main(void)
{
	int time = 20;

	(time < 10) ? printf("Good Morning") : printf("Good evening");
	return (0);
}
