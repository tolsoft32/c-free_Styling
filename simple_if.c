#include <stdio.h>
/**
 * main - Entry point
 * return success(0) - when code is successful
 */
int main(void)
{
	int x = 28;

	if (x <= 19)
	{
		printf("x is lesser than 20");
	}
	/* execute if condition not true */
	else
	{
		printf("x is greater than 20");
	}
	return (0);
}
