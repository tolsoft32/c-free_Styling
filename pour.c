#include <stdio.h>
/**
 * main - Entry point
 * return - sucees(0) as output
 */
int main(void)
{
	int c = 10;

	while (c > 0)
	{
		puts("I must not write code in class");
		c = c - 1;
	}
	return (0);
}
